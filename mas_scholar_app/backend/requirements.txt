# Core dependencies (required)
fastapi>=0.100.0
uvicorn[standard]
pydantic>=2.0.0
python-multipart
websockets
python-dotenv

# HTTP client for web search
httpx
duckduckgo-search==4.1.1

# Vector search - ENABLE_VECTOR_SEARCH=true for full hybrid search
# These are installed but only loaded at runtime if enabled
qdrant-client>=1.7.0
sentence-transformers>=2.2.0
